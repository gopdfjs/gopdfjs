[package]
name = "gopdf-image"
version.workspace = true
edition.workspace = true
description = "RGBA encode and PDF image grayscale (RFC 0017, 0018, 0028)"

[dependencies]
image = { version = "0.25", default-features = false, features = ["jpeg", "png", "webp"] }
