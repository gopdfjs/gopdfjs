{
  "name": "@gopdfjs/pdf-wasm",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "main": "./index.ts",
  "types": "./index.ts",
  "exports": {
    ".": "./index.ts",
    "./worker": "./worker.ts"
  },
  "scripts": {
    "test": "vitest run",
    "build:wasm": "pnpm -w run build:wasm",
    "build:dev": "pnpm -w run build:wasm:dev",
    "build": "pnpm run build:wasm && vite build",
    "clean": "rm -rf pkg dist"
  },
  "devDependencies": {
    "typescript": "^5",
    "vite": "^5",
    "vite-plugin-dts": "^3",
    "vite-plugin-top-level-await": "^1",
    "vite-plugin-wasm": "^3",
    "vitest": "^3.0.0"
  }
}
