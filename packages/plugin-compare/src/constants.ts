/** Default render scale — matches edit tool. */
export const COMPARE_DISPLAY_SCALE = 1.5;

/** Max pixels per rendered page (~5 MP per RFC 0053). */
export const COMPARE_MAX_PAGE_PIXELS = 5_000_000;

/** Sliding window radius (current ±1). */
export const COMPARE_PAGE_WINDOW_RADIUS = 1;

/** pixelmatch threshold. */
export const PIXELMATCH_THRESHOLD = 0.1;
