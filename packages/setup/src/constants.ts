/** MCP server key used in host config files. */
export const SERVER_NAME = "gopdf" as const;

/** Arguments passed to the gopdf binary for stdio MCP. */
export const MCP_ARGS = ["mcp"] as const;

/** JSON indent when writing config files. */
export const JSON_INDENT = 2 as const;
