/**
 * Browser PDF compare helpers — re-exported from `@gopdfjs/plugin-compare`.
 * Import via `@gopdfjs/engine/compare` (UI layer; not part of bytes-only `Gopdf` facade).
 */
export * from "@gopdfjs/plugin-compare";
