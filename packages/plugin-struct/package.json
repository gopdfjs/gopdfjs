{
  "name": "@gopdfjs/plugin-struct",
  "version": "0.1.0",
  "private": true,
  "description": "Structural PDF edits via pdf-lib (merge, split, rotate, forms, …). No env deps.",
  "type": "module",
  "main": "./src/index.ts",
  "types": "./src/index.ts",
  "exports": {
    ".": "./src/index.ts"
  },
  "scripts": {
    "test": "vitest run",
    "test:coverage": "vitest run --coverage"
  },
  "dependencies": {
    "@cantoo/pdf-lib": "^2.6.5",
    "pdf-lib": "^1.17.1"
  },
  "devDependencies": {
    "@vitest/browser": "^3.2.4",
    "@vitest/coverage-v8": "^3.0.0",
    "happy-dom": "^20.8.4",
    "typescript": "^5",
    "vitest": "^3.0.0"
  }
}
