/** Page raster scale for secure flatten (text removed from stream). */
export const REDACT_RENDER_SCALE = 2;

/** Padding around auto-detected text matches (PDF points). */
export const PATTERN_MATCH_PADDING_PT = 2;
