/** @deprecated Use `createEngine(adapter)` from `@gopdfjs/engine` + `@gopdfjs/runtime` types. */
export type {
  CanvasSurface,
  PdfDocument,
  PdfPage,
  PdfTextContentItem,
  PdfViewport,
} from "@gopdfjs/runtime/document";

export type { CanvasPort } from "@gopdfjs/adapter/render";
