{
  "name": "@gopdfjs/model",
  "version": "0.1.0",
  "private": true,
  "description": "Shared PDF document/canvas types — adapter ports and plugin runtime (zero env deps)",
  "type": "module",
  "exports": {
    ".": "./src/index.ts",
    "./document": "./src/document.ts"
  },
  "scripts": {
    "test": "vitest run"
  },
  "devDependencies": {
    "typescript": "^5",
    "vitest": "^3.0.0"
  },
  "license": "MIT"
}
